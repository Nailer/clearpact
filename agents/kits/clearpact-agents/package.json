{
  "name": "@clearpact/agents",
  "version": "0.0.0",
  "private": true,
  "description": "Buyer, worker, and verifier agents for ClearPact — Claude Agent SDK, acting through Circle Wallets on Arc testnet.",
  "type": "module",
  "engines": {
    "bun": ">=1.2"
  },
  "scripts": {
    "demo": "bun run src/demo.ts",
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.144",
    "dotenv": "^17.4.2",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.5.0"
  }
}
